# Test report — Digital Store (PR #1 в DSasdg/store)

Все тесты прогнал по плану из `test-plan.md`. Ниже фактические результаты.

## Окружение
- Дев-сервер: `http://127.0.0.1:8088` (PHP built-in, корень `store/`).
- БД: MariaDB локально, схема `digital_store`, импортирована из `dump.sql`.
- Учётки: `admin/admin123`, покупатель `ivanov/ivan123`.

## Результаты

| ID | Сценарий | Результат | Фактическое наблюдение |
|----|---|---|---|
| T1 | Шапка + мега-меню заменили горизонтальную полосу | PASS | На главной видна шапка с кнопкой «☰ Каталог». Клик открывает мега-меню с 16 плитками-категориями (4×4 на десктопе). См. `shot_megamenu.png`. |
| T2 | 28 товаров с чистыми фото на белом | PASS | `Найдено: 28`. Все 28 карточек одинакового формата (бренд-полоса + силуэт устройства + название/модель). `curl image.php?id=N` отдаёт `image/jpeg` 20–45 КБ для каждого N=1..28. |
| T3 | Поиск «macbook» → 1 товар | PASS | `Найдено: 1`, единственная карточка MacBook Pro 14, цена 219 990 ₽. |
| T4 | Фильтр «Смартфоны» → 5 товаров | PASS | `Найдено: 5` (iPhone 15 Pro, Galaxy S24 Ultra, Xiaomi 14 Pro, Pixel 8, Honor Magic 6 Pro). |
| T5 | Корзина → оформление → личный кабинет | PASS | Добавил iPhone 15 Pro в корзину (сессия), залогинился `ivanov/ivan123`, оформил заказ. DB: новый `orders.id=20`, `total=119990`, `status='Новый'`. На `account.php` flash «Заказ №20 успешно оформлен!» и в истории заказ №20. |
| T6 | Админ — загрузка фото — round-trip BLOB | PASS | Загрузил `/tmp/qa.jpg` (14 239 байт, SHA256 `24f19904…`) через `admin/product_edit.php?id=6`. DB: `OCTET_LENGTH(image)=14239`, `SHA2(image,256)='24f19904681410f14ec31f20cf00cd5a05b6bfb4d1025f1b81b8c7fac84cad31'`, `image_mime='image/jpeg'`. Совпадает байт-в-байт. После теста BLOB восстановлен на p6 card. |
| T7 | Смена статуса в админке → ЛК покупателя | PASS | В админке `order_view.php?id=20` сменил статус «Новый → Подтверждён». DB: `status='Подтверждён'`. На `account.php` под ivanov заказ №20 показывает статус «Подтверждён». |

## Регрессия

- SQL-injection: `index.php?q=' OR 1=1 --` → `Найдено: 0` (запрос работает, инъекция игнорируется благодаря prepared statements).
- Защита админки: `/admin/products.php` без сессии → HTTP 302 на `/admin/login.php`.

## Артефакты

- `shot_catalog.png` — главная страница, 28 карточек в новом стиле.
- `shot_megamenu.png` — открытое мега-меню.
- `shot_product_iphone.png` — карточка iPhone 15 Pro.
- `shot_product_pavilion.png` — карточка HP Pavilion 15 (ранее была заглушка с логотипом).

Все 7 тестов + 2 регрессионных проверки прошли успешно.
